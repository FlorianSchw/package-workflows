#### GitHub Action to increment the public-facing version based on semantic versioning v2

name: Release

on:
  workflow_call:
    inputs:
      config-repo:
        description: 'owner/repo holding the fallback .releaserc.json'
        required: false
        type: string
        default: 'FlorianSchw/package-workflows'
      config-ref:
        description: 'Ref of config-repo to read .releaserc.json from'
        required: false
        type: string
        default: 'v1'
      dry-run:
        description: 'Only report the next version and release notes; release nothing'
        required: false
        type: boolean
        default: false
    secrets:
      app-id:
        description: 'GitHub App ID; its token lets bot PRs trigger checks (optional).'
        required: false
      app-private-key:
        description: 'Private key of that GitHub App (required with app-id).'
        required: false
      commit-push-pat:
        description: 'PAT with contents write, fallback if no App is set (optional).'
        required: false

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0

      - name: Checkout shared release config
        uses: actions/checkout@v7
        with:
          repository: ${{ inputs.config-repo }}
          ref: ${{ inputs.config-ref }}
          path: .shared-config

      - uses: r-lib/actions/setup-r@v2

      # Only desc, for desc_set_version() in .releaserc.json's prepareCmd.
      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          packages: any::desc

      - uses: actions/setup-node@v7
        with:
          node-version: "lts/*"

      - name: Install semantic-release and plugins
        run: npm install semantic-release @semantic-release/git @semantic-release/changelog @semantic-release/exec

      - name: Verify npm registry signatures
        run: npm audit signatures

      - name: Resolve push token
        id: push_token
        uses: FlorianSchw/package-workflows/.github/actions/resolve-push-token@main
        with:
          app-id: ${{ secrets.app-id }}
          app-private-key: ${{ secrets.app-private-key }}

      - name: Release
        env:
          GITHUB_TOKEN: ${{ steps.push_token.outputs.token || secrets.commit-push-pat || secrets.GITHUB_TOKEN }}
        run: |
          ARGS=()
          if [ "${{ inputs.dry-run }}" = "true" ]; then ARGS+=(--dry-run); fi
          if [ ! -f .releaserc.json ]; then ARGS+=(--extends ./.shared-config/.releaserc.json); fi
          npx semantic-release "${ARGS[@]}"
